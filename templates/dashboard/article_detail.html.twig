{% extends 'base_dashboard.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-xl-6">
            {{ tmpl|raw }}
        </div>
    </div>
    <hr>
    <div class="row my-4">
        <div class="col-xl-6">
            <a href="{{ path('create_article', {id: id}) }}" class="btn btn-primary">Попробовать еще раз</a>
        </div>
    </div>

{% endblock %}



